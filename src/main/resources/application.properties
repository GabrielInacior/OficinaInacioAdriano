spring.application.name=OficinaInacioAdriano

# ========================
# PostgreSQL
# ========================
spring.datasource.url=jdbc:postgresql://localhost:5432/oficinaInacioAdriano
spring.datasource.username=postgres
spring.datasource.password=Inacio123
spring.datasource.driver-class-name=org.postgresql.Driver

# ========================
# JPA / Hibernate
# ========================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# ========================
# Flyway
# ========================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# ========================
# JWT
# ========================
app.jwt.secret=oficina-inacio-adriano-secret-key-hmac-sha256-minimum-256-bits-long-enough
app.jwt.expiration=86400000

# ========================
# Thymeleaf
# ========================
spring.thymeleaf.cache=false
spring.thymeleaf.check-template-location=false

# ========================
# Server
# ========================
server.port=8080

# ========================
# Email (SMTP Gmail)
# ========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=gabrielinaciodev47@gmail.com
spring.mail.password=lldh cjzw yodk mqmt
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

